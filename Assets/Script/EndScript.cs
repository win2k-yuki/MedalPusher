using UnityEngine;
using System.Collections;
using UnityEngine.UI;
public class EndScript : MonoBehaviour {
    Renderer EndObject;
	// Use this for initialization
	void Start () {
       //EndObject = this.GetComponent<Renderer>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
    void GameOver() {
     
    }
}
